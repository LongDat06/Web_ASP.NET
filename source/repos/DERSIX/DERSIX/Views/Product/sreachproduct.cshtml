@using DERSIX.Models;
@{
    Layout = "_Layout";
}

<div class="to">
    <form action="/Product/sreachproduct?name='sreach'">

        <input type="search" name="sreach" placeholder="Tìm kiếm" />
    </form>
</div>
<div class="small-container">

    <div class="row row-2">
        <h2>All Products</h2>
        <form action="/Product/Lipsick" method="post" enctype="multipart/form-data">

            <select name="abc">
                <option>All product</option>
                <option value="P">Lipsick</option>
                <option value="M">Mask</option>
                <option value="SC">Skin Care</option>
            </select>
            <input type="submit" value="Chọn">
        </form>

    </div>

    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-4">
                <form action="/Giohang/Add_cart?product_id=@item.Id" method="POST">
                    <img src="~/@item.Img">
                    <a href="/Product/deltail?id=@item.Id"><h4>@item.Name_products </h4></a>
                    <p>@item.Price VND</p>
                    <hidden idsp=@item.Id></hidden>
                    <input type="submit" name="submit" value="Chọn mua">

                </form>
            </div>
        }

    </div>
    
</div>